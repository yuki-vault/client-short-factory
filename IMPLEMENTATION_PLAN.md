# 案件用ショート動画工場：資産整理と実装計画

## 結論

既存資産を再利用し、案件用の決定論的なPython CLIを追加する構成で実現可能です。Hermes Agentは工程の指示とジョブ起動を担当し、取得・切り抜き・文字起こし・字幕生成・レンダリング・検査はCLI側へ固定します。

多人数ライブもMVPでは処理できます。ただし話者分離はせず、全員の発話を1本の字幕列として扱います。同時発話、固有名詞、ゲーム用語は誤認識が残るため、人間の字幕確認を納品前の必須工程にします。

## 確認できた既存資産

- `D:\HermesWorkspace\.hermes\skills\productivity\social-video-publishing`
  - 自撮り動画の検査、文字起こし、切り抜き、縦型化、字幕、音響、QAの実績手順
- `D:\HermesWorkspace\ro-brojp-social`
  - FFmpegによる切り抜き、1080×1920化、ASS/SRT焼き込み、BGM/SFX、完成MP4の実績
- `D:\HermesWorkspace\ai-github-publish-social\hermes_claudecode\remotion`
  - JSON駆動の縦型動画工場、セーフエリア、重要語装飾、音量QC
  - 現行検査は字幕占有率60%超を拒否するため、フルテロップMVPへ直接流用しない
- HermesのPython環境
  - `faster-whisper 1.2.1` とWhisperモデルキャッシュ
- WinGet版のyt-dlp、FFmpeg 8.1、ffprobe、libass、NVENC

## 新規に追加したもの

正式パスは `D:\HermesWorkspace\client-short-factory` です。

- URLまたはローカル動画、開始・終了時刻、字幕テンプレート、辞書、任意BGMを受け取るCLI
- YouTube直接区間取得と、失敗時の全編共有キャッシュへの自動フォールバック
- 画質上限別キャッシュ、映像・音声・尺・解像度の再利用前検査
- 正確なローカル切り抜き、16kHz音声抽出、faster-whisperの単語時刻付き文字起こし
- 日本語字幕分割、1〜2行、1行最大15文字、フィラー文字の除外、辞書補正
- SRT、ASS、文字起こし原稿、JSON、処理ログの保存
- 16:9全体を残すぼかし背景型9:16レイアウト
- ASSフルテロップ焼き込み、-14 LUFS正規化、1080×1920 H.264/AAC出力
- 工程別state.json、完了工程のスキップ、`--rerun-from`による指定工程以降の再実行
- 一時ファイル検証後の原子的置換による正常成果物の保護
- ffprobe、全編デコード、尺、コーデック、解像度、fps、字幕規則の機械QC
- 外部アップロードなし、URL利用時の権利確認フラグと許可メモ

## 今回の実証

- 入力：許可済みの7時間27分ライブアーカイブ
- 採用区間：`01:50:04.2–01:50:58.7`（54.5秒）
- 選定理由：質問から回答、作業方針までが短時間で成立し、2〜3人の交互発話を試せる
- 初回のみ720p全編をDドライブへキャッシュ（約5.5GB）
- 出力：1080×1920、30fps、H.264/AAC、フルテロップ、54.5秒
- 技術QC：合格
- 字幕：23枚、1〜2行、最短表示0.70秒、時刻重複なし

区間選定は今回Codexが複数候補を試聴用に抽出して決めたもので、汎用的な「AI見どころ自動選定」はまだ製品機能ではありません。

## 現在足りないもの

- 多人数・同時発話に対する高精度な話者分離
- 誤認識した字幕を短時間で直す専用レビュー画面
- フィラーや無音に対応した映像・音声そのものの削除
- 顔追跡、発話者追跡、自動画角移動
- 重要語のAI判定と部分色変更
- 全編文字起こしからの候補区間自動提案
- 複数ジョブのキュー、ロック、並列数制御
- 案件テンプレート作成UI、タイトル、概要欄、ハッシュタグ生成

## 実装優先順位

### Phase 1：納品可能な1本処理

1. 現行CLIで生成
2. SRTまたはASSを人間が確認・修正
3. `--rerun-from render`で文字起こしを再実行せず再書き出し
4. プレビューとqc.jsonを確認して承認

次に追加する価値が最も高いのは、動画を見ながら字幕本文と辞書を直し、再レンダリングできるローカル確認画面です。

### Phase 2：案件テンプレートと精度

- `config/templates/<client>.json`へフォント、色、縁、背景、位置、安全領域、音量、BGMを保存
- `config/dictionaries/<client>.json`へ人名、番組名、ゲーム用語を保存
- 重要語のASSタグ生成
- CUDA実行環境を整備し、8GB VRAMで `turbo/int8_float16` を実測。安定しなければ `medium`、速度優先なら `small`
- 必要な案件だけ話者分離を追加

実機GPUは申告のRTX 2080ではなくRTX 2070 SUPER 8GBでした。今回の54.5秒区間は、キャッシュ済みTurboをCPU int8で約61秒で文字起こしできています。

### Phase 3：候補選定と一括処理

- 全編は低負荷設定で一度文字起こしし、時間付き原稿を保存
- 30〜60秒で意味が閉じる区間、冒頭フック、同時発話の少なさを採点
- 複数候補を人間へ提示し、承認済み区間だけ現行CLIへ渡す
- ジョブキュー、案件別テンプレート、一括実行、失敗ジョブだけの再開を追加

## 人間確認を残して時間を減らす運用

機械側は取得、切り抜き、仮字幕、整形、画角、音量、焼き込み、技術QCまで行います。人間は候補区間の採否、誤字・固有名詞・同時発話、字幕の意味分割、最終映像だけを確認します。修正後はrender工程から再開するため、取得とWhisperを繰り返しません。

技術QCの合格は文字の意味が正しいことを保証しません。クラウドワークス等へ納品する前には、依頼者の表記ルールと原音を照合した最終確認が必要です。
